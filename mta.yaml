_schema-version: 3.3.0
ID: zempcudfioricustauth
description: A simple CAP project.
version: 1.0.0
modules:
- name: zempcudfioricustauth-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: zempcudfioricustauth-auth
  - name: zempcudfioricustauth-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: zempcudfioricustauth-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: zempcudfioricustauth-db
  parameters:
    buildpack: nodejs_buildpack
- name: zempcudfioricustauth-destination-content
  type: com.sap.application.content
  requires:
  - name: zempcudfioricustauth-destination-service
    parameters:
      content-target: true
  - name: zempcudfioricustauth_html_repo_host
    parameters:
      service-key:
        name: zempcudfioricustauth_html_repo_host-key
  - name: zempcudfioricustauth-auth
    parameters:
      service-key:
        name: zempcudfioricustauth-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zempcudfioriapprouter_zempcudfioricustauth_html_repo_host
          ServiceInstanceName: zempcudfioricustauth-html5-app-host-service
          ServiceKeyName: zempcudfioricustauth_html_repo_host-key
          sap.cloud.service: zempcudfioriapprouter
        - Authentication: OAuth2UserTokenExchange
          Name: zempcudfioriapprouter_zempcudfioricustauth_auth
          ServiceInstanceName: zempcudfioricustauth-auth
          ServiceKeyName: zempcudfioricustauth-auth-key
          sap.cloud.service: zempcudfioriapprouter
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: zempcudfioricustauth-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zempcudfioricustauth_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zcapmfioriauth.zip
      name: zcapmfioriauth
      target-path: resources/
- name: zcapmfioriauth
  type: html5
  path: app/zcapmfioriauth
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zempcudfioricustauth-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: zempcudfioricustauth-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: zempcudfioricustauth-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: zempcudfioricustauth-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zempcudfioricustauth-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: zempcudfioricustauth_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zempcudfioricustauth-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
